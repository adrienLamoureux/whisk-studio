export const styles = {
  panel: {
    position: "absolute",
    right: "calc(100% + 14px)",
    top: 0,
    bottom: 0,
    width: 240,
    background: "var(--skr-comp-bg)",
    border: "1px solid var(--skr-comp-border)",
    borderRadius: 12,
    boxShadow: "var(--skr-comp-shadow), var(--skr-glow)",
    backdropFilter: "blur(10px)",
    display: "flex",
    flexDirection: "column",
    overflow: "hidden",
    animation: "skr-bubble-in-side var(--skr-duration-normal) var(--skr-ease-out)",
  },
  panelFull: {
    flex: 1,
    minWidth: 0,
    background: "var(--skr-comp-bg)",
    borderLeft: "1px solid var(--skr-comp-border)",
    borderRadius: "0 12px 12px 0",
    display: "flex",
    flexDirection: "column",
    overflow: "hidden",
  },
  memoryRow: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    padding: "5px 10px",
    borderBottom: "1px solid var(--skr-comp-border-faint)",
    flexShrink: 0,
  },
  memoryBadge: {
    fontSize: 9,
    color: "var(--skr-text-secondary)",
    background: "var(--skr-comp-bubble-ai-bg)",
    border: "1px solid var(--skr-comp-bubble-ai-border)",
    borderRadius: 4,
    padding: "1px 5px",
    fontStyle: "italic",
  },
  forgetBtn: {
    background: "none",
    border: "1px solid color-mix(in srgb, var(--skr-accent-danger) 20%, transparent)",
    borderRadius: 4,
    color: "color-mix(in srgb, var(--skr-accent-danger) 70%, transparent)",
    cursor: "pointer",
    fontSize: 9,
    padding: "1px 5px",
    textTransform: "uppercase",
    letterSpacing: "0.04em",
  },
  list: {
    flex: 1,
    overflowY: "auto",
    padding: "8px 10px",
    display: "flex",
    flexDirection: "column",
    gap: 5,
    scrollbarWidth: "thin",
    scrollbarColor: "var(--skr-comp-scrollbar) transparent",
  },
  empty: {
    color: "var(--skr-text-muted)",
    fontSize: 11,
    textAlign: "center",
    marginTop: 16,
    fontStyle: "italic",
  },
  bubble: {
    display: "flex",
    flexDirection: "column",
    gap: 2,
    padding: "5px 9px",
    borderRadius: 8,
    fontSize: 12,
    lineHeight: 1.45,
    maxWidth: "92%",
  },
  bubbleHiyori: {
    alignSelf: "flex-start",
    background: "var(--skr-comp-bubble-ai-bg)",
    border: "1px solid var(--skr-comp-bubble-ai-border)",
    color: "var(--skr-text)",
  },
  bubbleUser: {
    alignSelf: "flex-end",
    background: "var(--skr-comp-bubble-user-bg)",
    border: "1px solid var(--skr-comp-bubble-user-border)",
    color: "var(--skr-text)",
  },
  bubbleError: { opacity: 0.6, fontStyle: "italic" },
  name: { fontSize: 9, color: "var(--skr-accent)", fontWeight: 600, letterSpacing: "0.05em" },
  cursor: { animation: "skr-blink 1s step-end infinite", color: "var(--skr-accent)" },
  actionWrapper: { marginTop: 3 },
  inputRow: {
    display: "flex",
    gap: 5,
    padding: "7px 8px",
    borderTop: "1px solid var(--skr-comp-border-faint)",
    flexShrink: 0,
  },
  input: {
    flex: 1,
    background: "var(--skr-comp-input-bg)",
    border: "1px solid var(--skr-comp-input-border)",
    borderRadius: 6,
    padding: "5px 8px",
    color: "var(--skr-text)",
    fontSize: 11,
    outline: "none",
  },
  sendBtn: {
    background: "var(--skr-comp-send-bg)",
    border: "1px solid var(--skr-comp-send-border)",
    borderRadius: 6,
    color: "var(--skr-accent)",
    cursor: "pointer",
    fontSize: 15,
    fontWeight: 700,
    padding: "2px 10px",
    transition: "background var(--skr-duration-fast) var(--skr-ease-out)",
    lineHeight: 1,
  },
  // Two-triangle bordered tail pointing RIGHT toward the character
  tailOuter: {
    position: "absolute",
    right: -9,
    top: "50%",
    transform: "translateY(-50%)",
    width: 0,
    height: 0,
    borderTop: "8px solid transparent",
    borderBottom: "8px solid transparent",
    borderLeft: "9px solid var(--skr-comp-border)",
    pointerEvents: "none",
  },
  tailInner: {
    position: "absolute",
    right: -7,
    top: "50%",
    transform: "translateY(-50%)",
    width: 0,
    height: 0,
    borderTop: "7px solid transparent",
    borderBottom: "7px solid transparent",
    borderLeft: "8px solid var(--skr-comp-bg)",
    pointerEvents: "none",
  },
};
